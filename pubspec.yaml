name: hello_stranger
description: 畢業專題 App
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.1-alpha

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^1.17.1 # Firebase
  firebase_auth: ^3.3.19 # Firebase 手機認證
  package_info_plus: ^1.4.2 # 取得版本號
  flutter_spinkit: ^5.1.0 # 按鈕載入動畫
  pin_code_fields: ^7.4.0 # 手機認證碼區塊

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/app_logo_foreground.png
    - assets/default_account_photo.png
